# Inserção Ordenada Decrescente em Array (Java)

Este método realiza a inserção de um valor em um array **mantendo a ordenação decrescente** (do maior para o menor), utilizando uma abordagem manual com deslocamento de elementos.

## 📌 Assinatura do Método

```java
public static int inserirOrdenadoDecrescente(int[] lista, int tamanhoLogico, int novoValor)
